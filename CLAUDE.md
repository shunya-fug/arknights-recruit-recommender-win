# プロジェクト方針

## 言語

このプロジェクトでは基本的に日本語を使用する。

- コード中のコメントは日本語で書く（クラス名・メソッド名・変数名などの識別子は通常の.NET命名規則に従い英語のままでよい）。
- コミットメッセージ、Issue、ドキュメント（README等）も日本語で書く。
- コード生成・編集を行う際は、既存のコメント・ドキュメントの言語（日本語）に合わせること。

## ブランチ運用

- 基本は`develop`ブランチ1本で、機能を直列に実装していく。
- `main`へのマージは、アプリとして公開できるレベルに機能が仕上がった時点で行う。現時点では
  基本機能がまだ完成していないため、`main`へのマージは不要。
- 場合によっては`feature/*`ブランチを併用することもある。
