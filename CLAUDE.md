# プロジェクト方針

## 言語

このプロジェクトでは基本的に日本語を使用する。

- コード中のコメントは日本語で書く（クラス名・メソッド名・変数名などの識別子は通常の.NET命名規則に従い英語のままでよい）。
- コミットメッセージ、Issue、ドキュメント（README等）も日本語で書く。
- コード生成・編集を行う際は、既存のコメント・ドキュメントの言語（日本語）に合わせること。

## ブランチ運用

- 基本は`develop`ブランチ1本で、機能を直列に実装していく。
- `main`は「アプリとして公開できるレベル」のスナップショットを保持するブランチ。基本機能が
  一通り完成し実機検証も済んだ時点で最初のマージを行った（2026年8月）。以後も、develop側で
  意味のあるまとまり（不具合修正・機能追加）ができた区切りで、都度PR経由でmainへ反映していく。
- develop→mainの反映は、都度`gh pr create`でPRを作成し、CI(`ci.yml`)のパスを確認してから
  マージする（マージコミット方式。develop側の個々のコミット履歴を保持するため、squashはしない）。
  レビュワーはいないためセルフマージでよい。
- 場合によっては`feature/*`ブランチを併用することもある。

## コードレビューの運用

- コミット前にコードレビュー（`/code-review`スキル等）を行うのが基本だが、対象はロジックを
  含むコード変更に限る。ドキュメントのみの変更（README、LICENSE、免責事項の文言など）は
  レビュー不要（正誤ではなく「言いたいことが書けているか」の確認で十分なため）。

## GitHub Issueの運用

- Issueは「今すぐ対応しない・後回しにする作業」を記録するために使う。今この場で完結する
  軽微な作業（例: LICENSEファイルの追加）にはIssueを作らず直接対応してよい。
- 「今後何か気になることがあれば記録する」といった際限のない受け皿的なIssueは作らない
  （自然にクローズするタイミングが無く、見通しが悪くなるため）。具体的な事象が実際に
  発生した時点で、その都度スコープを絞った新規Issueを作成する。
- Issueには内容に応じたラベルを付ける（`bug`/`enhancement`等の標準ラベルで足りない場合は
  新規作成してよい。例: 実機での動作確認が必要な項目向けに`検証`ラベルを作成済み）。
